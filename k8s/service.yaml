apiVersion: v1
kind: Service
metadata:
  name: trend-service
  namespace: trend-app
spec:
  selector:
    app: trend-app    # 🔑 match pod label exactly
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80   # matches Nginx container port
  type: NodePort
